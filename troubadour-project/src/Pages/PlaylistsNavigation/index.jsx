function PlaylistsNavigation (){
    let mood = window.localStorage.getItem("mood")
    let userId = window.localStorage.getItem("userId")
   return (<div>hi!
    {/*Use local storage to save the params. create new params via the local storage and pass it on*/}

    </div>)
}

export default PlaylistsNavigation;